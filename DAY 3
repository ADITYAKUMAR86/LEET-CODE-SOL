class Solution {
public:
    int singleNonDuplicate(vector<int>& nums) {
        
        // long long max =*max_element(nums.begin(), nums.end());
        // vector<long long> hasharr {max + 1, 0};

        // for(long long i = 0; i < nums.size() + 1 ; i++){
        //     hasharr[nums[i]]++;
        // }

        // for(int i = 0 ; i < nums.size() + 1; i++){
        //     if (hasharr[i] == 1) return i;
        // }
        // return -1;

        unordered_map<int , int> mpp;
        for(int i = 0; i < nums.size() ; i++){
            // if(mpp.find(nums[i]) != mpp.end()){
            //     mpp[nums[i]]++;
            // }
            mpp[nums[i]]++;
        }

        for(int i = 0; i < nums.size(); i++){
            if(mpp[nums[i]] == 1) return nums[i];
        }
        return -1;
    }
};
